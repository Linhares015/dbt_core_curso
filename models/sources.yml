version: 2

sources:
  - name: raw
    description: Synthetic raw operational exports loaded locally through dbt seeds.
    schema: main
    tables:
      - name: raw_customers
      - name: raw_orders
      - name: raw_order_items
      - name: raw_products
